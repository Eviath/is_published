class Create<%= model_name.capitalize.pluralize %> < ActiveRecord::Migration
  def change
    create_table :<%= model_name.underscore.pluralize %> do |t|
      t.string :title
      t.string :body
      t.boolean :published, default: false
      t.timestamps
    end
  end
end
